/**
 * Firebase Service Removed.
 * This file is kept as a placeholder to prevent build errors in case of lingering imports,
 * but the application now uses Local Storage.
 */

export const db = null;
export const isConfigured = false;
